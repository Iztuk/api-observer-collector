# API Observer Collector

Collector is a lightweight log collection and indexing agent that monitors structured log sources, builds indexes to support efficient searching, and streams normalized HTTP events to API Observer for auditing and analysis.
